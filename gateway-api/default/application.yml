ribbon: 
  ConnectTimeout: 3000
  ReadTimeout: 60000
  maxAutoRetries: 1
  MaxAutoRetriesNextServer: 2
  eureka:
    enabled: true
    
zuul:
  #prefix: /api
  routes:
    ignoredService: '*'
    demo-service:
      path: /demo/**
      serviceId: demo-service
      stripPrefix: false
    #merchant-service:
    #  path: /merchants/**
    #  serviceId: merchant-service
    #  stripPrefix: true
