ribbon:
  ReadTimeout: 60000
  ConnectTimeout: 3000
zuul:
  routes:
    ignoredService: '*'
    demo-service:
      path: /demo/**
      serviceId: demo-service
      stripPrefix: false
    #merchant-service:
    #  path: /merchants/**
    #  serviceId: merchant-service
    #  stripPrefix: true
